{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",  // This ensures TypeScript looks inside the 'src' folder for imports
    "paths": {
      "*": ["src/*"]
    }
  },
   "include": [
    "src/**/*"
   ]
}
